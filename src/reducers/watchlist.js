const initialState = {
  inWatchList: false,
};

const watchlistReducer = (state = initialState, action = {}) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default watchlistReducer;
